FROM node:18-alpine 

WORKDIR /app

COPY package*.json ./

RUN npm install

COPY . .

# Generar Prisma client durante el build
RUN npx prisma generate

EXPOSE 3000

# Script que espera la DB y luego inicia la app
CMD ["sh", "-c", "npx prisma db push && npm run dev"]